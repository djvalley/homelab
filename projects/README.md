# projects/

Self-contained homelab coding projects. Each project gets its own subdirectory with its own README, dependencies, and repo (if applicable).

## Convention

```
projects/
└── my-project/
    ├── README.md
    ├── .env.example   # committed — never commit .env
    └── src/
```
