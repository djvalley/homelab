# configs/

Service configuration templates. These are reference/template files — live configs stay with their respective services.

Commit templates here, never live configs with secrets.
Use `.example` suffix for files that require local customization (e.g. `nginx.conf.example`).
